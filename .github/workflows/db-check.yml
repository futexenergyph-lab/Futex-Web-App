name: DB Check

# Diagnostic: verify the quotations list + Create Contract page render.

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      SAT: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      REF_IN: ${{ secrets.SUPABASE_PROJECT_REF }}
    steps:
      - name: Inspect
        run: |
          set +e
          REF="$REF_IN"
          if [ -z "$REF" ]; then
            REF=$(curl -s https://api.supabase.com/v1/projects -H "Authorization: Bearer $SAT" | jq -r 'if type=="array" then (.[0].id // empty) else empty end')
          fi
          URL="https://$REF.supabase.co"
          APP="https://futex-web-app.vercel.app"
          SERVICE=$(curl -s "https://api.supabase.com/v1/projects/$REF/api-keys" -H "Authorization: Bearer $SAT" | jq -r 'if type=="array" then (.[]|select(.name=="service_role")|.api_key) else empty end')
          ANON=$(curl -s "https://api.supabase.com/v1/projects/$REF/api-keys" -H "Authorization: Bearer $SAT" | jq -r 'if type=="array" then (.[]|select(.name=="anon")|.api_key) else empty end')

          QID=$(curl -s -G "$URL/rest/v1/quotations" --data-urlencode "select=id" --data-urlencode "limit=1" -H "apikey: $SERVICE" -H "Authorization: Bearer $SERVICE" | jq -r '.[0].id // empty')
          echo "sample quotation id: $QID"

          TS=$(date +%s%N)
          EMAIL="diag-admin-$TS@example.com"; PASS="Probe-$TS-Aa1!"
          CREATE=$(curl -s -X POST "$URL/auth/v1/admin/users" -H "apikey: $SERVICE" -H "Authorization: Bearer $SERVICE" -H "Content-Type: application/json" -d "{\"email\":\"$EMAIL\",\"password\":\"$PASS\",\"email_confirm\":true}")
          PUID=$(echo "$CREATE" | jq -r '.id // .user.id // empty')
          curl -s -X POST "$URL/rest/v1/profiles" -H "apikey: $SERVICE" -H "Authorization: Bearer $SERVICE" -H "Content-Type: application/json" -H "Prefer: resolution=merge-duplicates,return=minimal" -d "{\"id\":\"$PUID\",\"full_name\":\"Diag Admin\",\"role\":\"admin\",\"active\":true}" -o /dev/null
          TOK=$(curl -s -X POST "$URL/auth/v1/token?grant_type=password" -H "apikey: $ANON" -H "Content-Type: application/json" -d "{\"email\":\"$EMAIL\",\"password\":\"$PASS\"}")
          SESSION=$(echo "$TOK" | jq -c '.'); NAME="sb-$REF-auth-token"
          B64=$(printf '%s' "$SESSION" | base64 -w0 | tr '+/' '-_' | tr -d '='); VAL="base64-$B64"; LEN=${#VAL}
          COOKIE_ARGS=()
          if [ $LEN -le 3600 ]; then COOKIE_ARGS=(-b "$NAME=$VAL"); else
            off=0; i=0; while [ $off -lt $LEN ]; do COOKIE_ARGS+=(-b "$NAME.$i=${VAL:$off:3600}"); off=$((off+3600)); i=$((i+1)); done
          fi

          c1=$(curl -s "${COOKIE_ARGS[@]}" -o /tmp/a -w '%{http_code}' "$APP/admin/quotations")
          echo "/admin/quotations -> HTTP $c1  'Create a Contract'=$(grep -c 'Create a Contract' /tmp/a)"
          if [ -n "$QID" ]; then
            c2=$(curl -s "${COOKIE_ARGS[@]}" -o /tmp/b -w '%{http_code}' "$APP/admin/quotations/$QID/contract")
            echo "/admin/quotations/$QID/contract -> HTTP $c2  'Create Contract'=$(grep -c 'Create Contract' /tmp/b)  'Generate contract'=$(grep -c 'Generate contract' /tmp/b)"
          fi
          curl -s -X DELETE "$URL/auth/v1/admin/users/$PUID" -H "apikey: $SERVICE" -H "Authorization: Bearer $SERVICE" -o /dev/null
